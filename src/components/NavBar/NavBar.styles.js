import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  navItemRoot: {
    color: "#8B8B8B",
    listStyle: "none",
    fontWeight: "500",
    fontSize: "16px",
    fontFamily: "Noto Sans",
    cursor: "pointer",
  },
});
